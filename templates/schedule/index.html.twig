<h1>Alle Schedules</h1>

<ul>
    {% for schedule in schedules %}
        <li>
            <a href="/schedule/{{ schedule.id }}">
                {{ schedule.title }}
            </a>
            <p>{{ schedule.description }}</p>
        </li>
    {% endfor %}
</ul>
